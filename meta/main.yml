galaxy_info:
  author: Karol Ossowski
  description: uwsgi app in nginx container
  license: BSD

  min_ansible_version: 1.2

  platforms:
  - name: Debian
    versions:
    - all

  galaxy_tags: [nginx, uwsgi, debian]

dependencies:
  - role: nginx_base
